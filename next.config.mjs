/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/next-magic-portfolio-2025",
  assetPrefix: "/next-magic-portfolio-2025",
  output: "export",
  reactStrictMode: true,
};

export default nextConfig;
